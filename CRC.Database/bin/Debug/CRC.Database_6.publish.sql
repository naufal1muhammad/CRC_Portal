/*
Deployment script for CRC_DB

This code was generated by a tool.
Changes to this file may cause incorrect behavior and will be lost if
the code is regenerated.
*/

GO
SET ANSI_NULLS, ANSI_PADDING, ANSI_WARNINGS, ARITHABORT, CONCAT_NULL_YIELDS_NULL, QUOTED_IDENTIFIER ON;

SET NUMERIC_ROUNDABORT OFF;


GO
:setvar DatabaseName "CRC_DB"
:setvar DefaultFilePrefix "CRC_DB"
:setvar DefaultDataPath "C:\Program Files\Microsoft SQL Server\MSSQL16.MSSQLSERVER\MSSQL\DATA\"
:setvar DefaultLogPath "C:\Program Files\Microsoft SQL Server\MSSQL16.MSSQLSERVER\MSSQL\DATA\"

GO
:on error exit
GO
/*
Detect SQLCMD mode and disable script execution if SQLCMD mode is not supported.
To re-enable the script after enabling SQLCMD mode, execute the following:
SET NOEXEC OFF; 
*/
:setvar __IsSqlCmdEnabled "True"
GO
IF N'$(__IsSqlCmdEnabled)' NOT LIKE N'True'
    BEGIN
        PRINT N'SQLCMD mode must be enabled to successfully execute this script.';
        SET NOEXEC ON;
    END


GO
USE [$(DatabaseName)];


GO
PRINT N'Creating Procedure [dbo].[spAsset_Delete]...';


GO
CREATE PROCEDURE [dbo].[spAsset_Delete]
    @Asset_ID VARCHAR(100)
AS
BEGIN
    SET NOCOUNT ON;

    DELETE FROM [dbo].[Asset]
    WHERE [Asset_ID] = @Asset_ID;
END;
GO
PRINT N'Creating Procedure [dbo].[spAsset_GetById]...';


GO
CREATE PROCEDURE [dbo].[spAsset_GetById]
    @Asset_ID VARCHAR(100)
AS
BEGIN
    SET NOCOUNT ON;

    SELECT TOP 1
        [Asset_ID],
        [Asset_Name],
        [Branch_ID],
        [Branch_Name],
        [Asset_Quantity],
        [Asset_Cost],
        [Asset_TotalCost]
    FROM [dbo].[Asset]
    WHERE [Asset_ID] = @Asset_ID;
END;
GO
PRINT N'Creating Procedure [dbo].[spAsset_Insert]...';


GO
CREATE PROCEDURE [dbo].[spAsset_Insert]
    @Asset_ID        VARCHAR(100),
    @Asset_Name      VARCHAR(100),
    @Branch_ID       VARCHAR(100),
    @Branch_Name     VARCHAR(100),
    @Asset_Quantity  INT,
    @Asset_Cost      DECIMAL(18, 2)
AS
BEGIN
    SET NOCOUNT ON;

    IF EXISTS (SELECT 1 FROM [dbo].[Asset] WHERE [Asset_ID] = @Asset_ID)
    BEGIN
        RAISERROR('Asset ID already exists.', 16, 1);
        RETURN;
    END

    INSERT INTO [dbo].[Asset] (
        [Asset_ID],
        [Asset_Name],
        [Branch_ID],
        [Branch_Name],
        [Asset_Quantity],
        [Asset_Cost]
        -- Asset_TotalCost is computed
    )
    VALUES (
        @Asset_ID,
        @Asset_Name,
        @Branch_ID,
        @Branch_Name,
        @Asset_Quantity,
        @Asset_Cost
    );
END;
GO
PRINT N'Creating Procedure [dbo].[spAsset_ListByBranch]...';


GO
CREATE PROCEDURE [dbo].[spAsset_ListByBranch]
    @Branch_ID VARCHAR(100)
AS
BEGIN
    SET NOCOUNT ON;

    SELECT
        [Asset_ID],
        [Asset_Name],
        [Branch_ID],
        [Branch_Name],
        [Asset_Quantity],
        [Asset_Cost],
        [Asset_TotalCost]
    FROM [dbo].[Asset]
    WHERE [Branch_ID] = @Branch_ID
    ORDER BY [Asset_Name];
END;
GO
PRINT N'Creating Procedure [dbo].[spAsset_Update]...';


GO
CREATE PROCEDURE [dbo].[spAsset_Update]
    @Asset_ID        VARCHAR(100),
    @Asset_Name      VARCHAR(100),
    @Branch_ID       VARCHAR(100),
    @Branch_Name     VARCHAR(100),
    @Asset_Quantity  INT,
    @Asset_Cost      DECIMAL(18, 2)
AS
BEGIN
    SET NOCOUNT ON;

    IF NOT EXISTS (SELECT 1 FROM [dbo].[Asset] WHERE [Asset_ID] = @Asset_ID)
    BEGIN
        RAISERROR('Asset not found.', 16, 1);
        RETURN;
    END

    UPDATE [dbo].[Asset]
    SET
        [Asset_Name]     = @Asset_Name,
        [Branch_ID]      = @Branch_ID,
        [Branch_Name]    = @Branch_Name,
        [Asset_Quantity] = @Asset_Quantity,
        [Asset_Cost]     = @Asset_Cost
    WHERE [Asset_ID] = @Asset_ID;
END;
GO
PRINT N'Creating Procedure [dbo].[spBranch_Delete]...';


GO
CREATE PROCEDURE [dbo].[spBranch_Delete]
    @Branch_ID VARCHAR(100)
AS
BEGIN
    SET NOCOUNT ON;

    DELETE FROM [dbo].[Branch]
    WHERE [Branch_ID] = @Branch_ID;
END;
GO
PRINT N'Creating Procedure [dbo].[spBranch_GetById]...';


GO
CREATE PROCEDURE [dbo].[spBranch_GetById]
    @Branch_ID VARCHAR(100)
AS
BEGIN
    SET NOCOUNT ON;

    SELECT TOP 1
        [Branch_ID],
        [Branch_Name],
        [Branch_Location],
        [Branch_State],
        [Branch_Status]
    FROM [dbo].[Branch]
    WHERE [Branch_ID] = @Branch_ID;
END;
GO
PRINT N'Creating Procedure [dbo].[spBranch_Insert]...';


GO
CREATE PROCEDURE [dbo].[spBranch_Insert]
    @Branch_ID        VARCHAR(100),
    @Branch_Name      VARCHAR(100),
    @Branch_Location  VARCHAR(200),
    @Branch_State     VARCHAR(100),
    @Branch_Status    BIT
AS
BEGIN
    SET NOCOUNT ON;

    IF EXISTS (SELECT 1 FROM [dbo].[Branch] WHERE [Branch_ID] = @Branch_ID)
    BEGIN
        RAISERROR('Branch ID already exists.', 16, 1);
        RETURN;
    END

    INSERT INTO [dbo].[Branch] (
        [Branch_ID],
        [Branch_Name],
        [Branch_Location],
        [Branch_State],
        [Branch_Status]
    )
    VALUES (
        @Branch_ID,
        @Branch_Name,
        @Branch_Location,
        @Branch_State,
        @Branch_Status
    );
END;
GO
PRINT N'Creating Procedure [dbo].[spBranch_ListAll]...';


GO
CREATE PROCEDURE [dbo].[spBranch_ListAll]
AS
BEGIN
    SET NOCOUNT ON;

    SELECT
        [Branch_ID],
        [Branch_Name],
        [Branch_Location],
        [Branch_State],
        [Branch_Status]
    FROM [dbo].[Branch]
    ORDER BY [Branch_Name];
END;
GO
PRINT N'Creating Procedure [dbo].[spBranch_Update]...';


GO
CREATE PROCEDURE [dbo].[spBranch_Update]
    @Branch_ID        VARCHAR(100),
    @Branch_Name      VARCHAR(100),
    @Branch_Location  VARCHAR(200),
    @Branch_State     VARCHAR(100),
    @Branch_Status    BIT
AS
BEGIN
    SET NOCOUNT ON;

    IF NOT EXISTS (SELECT 1 FROM [dbo].[Branch] WHERE [Branch_ID] = @Branch_ID)
    BEGIN
        RAISERROR('Branch not found.', 16, 1);
        RETURN;
    END

    UPDATE [dbo].[Branch]
    SET
        [Branch_Name]     = @Branch_Name,
        [Branch_Location] = @Branch_Location,
        [Branch_State]    = @Branch_State,
        [Branch_Status]   = @Branch_Status
    WHERE [Branch_ID] = @Branch_ID;
END;
GO
PRINT N'Update complete.';


GO

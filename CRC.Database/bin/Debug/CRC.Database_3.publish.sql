/*
Deployment script for CRC_DB

This code was generated by a tool.
Changes to this file may cause incorrect behavior and will be lost if
the code is regenerated.
*/

GO
SET ANSI_NULLS, ANSI_PADDING, ANSI_WARNINGS, ARITHABORT, CONCAT_NULL_YIELDS_NULL, QUOTED_IDENTIFIER ON;

SET NUMERIC_ROUNDABORT OFF;


GO
:setvar DatabaseName "CRC_DB"
:setvar DefaultFilePrefix "CRC_DB"
:setvar DefaultDataPath "C:\Program Files\Microsoft SQL Server\MSSQL16.MSSQLSERVER\MSSQL\DATA\"
:setvar DefaultLogPath "C:\Program Files\Microsoft SQL Server\MSSQL16.MSSQLSERVER\MSSQL\DATA\"

GO
:on error exit
GO
/*
Detect SQLCMD mode and disable script execution if SQLCMD mode is not supported.
To re-enable the script after enabling SQLCMD mode, execute the following:
SET NOEXEC OFF; 
*/
:setvar __IsSqlCmdEnabled "True"
GO
IF N'$(__IsSqlCmdEnabled)' NOT LIKE N'True'
    BEGIN
        PRINT N'SQLCMD mode must be enabled to successfully execute this script.';
        SET NOEXEC ON;
    END


GO
USE [$(DatabaseName)];


GO
PRINT N'Creating Procedure [dbo].[spBranch_ListActive]...';


GO
CREATE PROCEDURE [dbo].[spBranch_ListActive]
AS
BEGIN
    SET NOCOUNT ON;

    SELECT [Branch_ID], [Branch_Name]
    FROM [dbo].[Branch]
    WHERE [Branch_Status] = 1
    ORDER BY [Branch_Name];
END;
GO
PRINT N'Creating Procedure [dbo].[spPatient_Delete]...';


GO
CREATE PROCEDURE [dbo].[spPatient_Delete]
    @Patient_ID VARCHAR(100)
AS
BEGIN
    SET NOCOUNT ON;

    DELETE FROM [dbo].[Patient]
    WHERE [Patient_ID] = @Patient_ID;
END;
GO
PRINT N'Creating Procedure [dbo].[spPatient_GetById]...';


GO
CREATE PROCEDURE [dbo].[spPatient_GetById]
    @Patient_ID VARCHAR(100)
AS
BEGIN
    SET NOCOUNT ON;

    SELECT TOP 1
        [Patient_ID],
        [Patient_Name],
        [Patient_NRIC],
        [Patient_Phone],
        [Patient_Email],
        [Branch_ID],
        [Branch_Name],
        [Patient_Stage],
        [Patient_Remarks],
        [Appointment_Date]
    FROM [dbo].[Patient]
    WHERE [Patient_ID] = @Patient_ID;
END;
GO
PRINT N'Creating Procedure [dbo].[spPatient_Insert]...';


GO
CREATE PROCEDURE [dbo].[spPatient_Insert]
    @Patient_ID       VARCHAR(100),
    @Patient_Name     VARCHAR(100),
    @Patient_NRIC     VARCHAR(100),
    @Patient_Phone    VARCHAR(50),
    @Patient_Email    VARCHAR(100),
    @Branch_ID        VARCHAR(100),
    @Branch_Name      VARCHAR(100),
    @Patient_Stage    VARCHAR(10),
    @Patient_Remarks  VARCHAR(MAX),
    @Appointment_Date DATETIME2
AS
BEGIN
    SET NOCOUNT ON;

    IF EXISTS (SELECT 1 FROM [dbo].[Patient] WHERE [Patient_ID] = @Patient_ID)
    BEGIN
        RAISERROR('Patient ID already exists.', 16, 1);
        RETURN;
    END

    INSERT INTO [dbo].[Patient] (
        [Patient_ID],
        [Patient_Name],
        [Patient_NRIC],
        [Patient_Phone],
        [Patient_Email],
        [Branch_ID],
        [Branch_Name],
        [Patient_Stage],
        [Patient_Remarks],
        [Appointment_Date]
    )
    VALUES (
        @Patient_ID,
        @Patient_Name,
        @Patient_NRIC,
        @Patient_Phone,
        @Patient_Email,
        @Branch_ID,
        @Branch_Name,
        @Patient_Stage,
        @Patient_Remarks,
        @Appointment_Date
    );
END;
GO
PRINT N'Creating Procedure [dbo].[spPatient_ListByStage]...';


GO
CREATE PROCEDURE [dbo].[spPatient_ListByStage]
    @Stage VARCHAR(10)
AS
BEGIN
    SET NOCOUNT ON;

    SELECT
        [Patient_ID],
        [Patient_Name],
        [Patient_NRIC],
        [Patient_Phone],
        [Patient_Email],
        [Branch_ID],
        [Branch_Name],
        [Patient_Stage],
        [Patient_Remarks],
        [Appointment_Date]
    FROM [dbo].[Patient]
    WHERE [Patient_Stage] = @Stage
    ORDER BY [Patient_Name];
END;
GO
PRINT N'Creating Procedure [dbo].[spPatient_Update]...';


GO
CREATE PROCEDURE [dbo].[spPatient_Update]
    @Patient_ID       VARCHAR(100),
    @Patient_Name     VARCHAR(100),
    @Patient_NRIC     VARCHAR(100),
    @Patient_Phone    VARCHAR(50),
    @Patient_Email    VARCHAR(100),
    @Branch_ID        VARCHAR(100),
    @Branch_Name      VARCHAR(100),
    @Patient_Stage    VARCHAR(10),
    @Patient_Remarks  VARCHAR(MAX),
    @Appointment_Date DATETIME2
AS
BEGIN
    SET NOCOUNT ON;

    IF NOT EXISTS (SELECT 1 FROM [dbo].[Patient] WHERE [Patient_ID] = @Patient_ID)
    BEGIN
        RAISERROR('Patient not found.', 16, 1);
        RETURN;
    END

    UPDATE [dbo].[Patient]
    SET
        [Patient_Name] = @Patient_Name,
        [Patient_NRIC] = @Patient_NRIC,
        [Patient_Phone] = @Patient_Phone,
        [Patient_Email] = @Patient_Email,
        [Branch_ID] = @Branch_ID,
        [Branch_Name] = @Branch_Name,
        [Patient_Stage] = @Patient_Stage,
        [Patient_Remarks] = @Patient_Remarks,
        [Appointment_Date] = @Appointment_Date
    WHERE [Patient_ID] = @Patient_ID;
END;
GO
PRINT N'Update complete.';


GO

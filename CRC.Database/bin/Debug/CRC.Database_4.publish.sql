/*
Deployment script for CRC_DB

This code was generated by a tool.
Changes to this file may cause incorrect behavior and will be lost if
the code is regenerated.
*/

GO
SET ANSI_NULLS, ANSI_PADDING, ANSI_WARNINGS, ARITHABORT, CONCAT_NULL_YIELDS_NULL, QUOTED_IDENTIFIER ON;

SET NUMERIC_ROUNDABORT OFF;


GO
:setvar DatabaseName "CRC_DB"
:setvar DefaultFilePrefix "CRC_DB"
:setvar DefaultDataPath "C:\Program Files\Microsoft SQL Server\MSSQL16.MSSQLSERVER\MSSQL\DATA\"
:setvar DefaultLogPath "C:\Program Files\Microsoft SQL Server\MSSQL16.MSSQLSERVER\MSSQL\DATA\"

GO
:on error exit
GO
/*
Detect SQLCMD mode and disable script execution if SQLCMD mode is not supported.
To re-enable the script after enabling SQLCMD mode, execute the following:
SET NOEXEC OFF; 
*/
:setvar __IsSqlCmdEnabled "True"
GO
IF N'$(__IsSqlCmdEnabled)' NOT LIKE N'True'
    BEGIN
        PRINT N'SQLCMD mode must be enabled to successfully execute this script.';
        SET NOEXEC ON;
    END


GO
USE [$(DatabaseName)];


GO
PRINT N'Creating Procedure [dbo].[spPatientDocument_Delete]...';


GO
CREATE PROCEDURE [dbo].[spPatientDocument_Delete]
    @PatientDocument_ID INT
AS
BEGIN
    SET NOCOUNT ON;

    DELETE FROM [dbo].[PatientDocument]
    WHERE [PatientDocument_ID] = @PatientDocument_ID;
END;
GO
PRINT N'Creating Procedure [dbo].[spPatientDocument_GetById]...';


GO
CREATE PROCEDURE [dbo].[spPatientDocument_GetById]
    @PatientDocument_ID INT
AS
BEGIN
    SET NOCOUNT ON;

    SELECT TOP 1
        [PatientDocument_ID],
        [Patient_ID],
        [FileName],
        [FilePath],
        [ContentType],
        [UploadedOn]
    FROM [dbo].[PatientDocument]
    WHERE [PatientDocument_ID] = @PatientDocument_ID;
END;
GO
PRINT N'Creating Procedure [dbo].[spPatientDocument_Insert]...';


GO
CREATE PROCEDURE [dbo].[spPatientDocument_Insert]
    @Patient_ID VARCHAR(100),
    @FileName   VARCHAR(255),
    @FilePath   VARCHAR(500),
    @ContentType VARCHAR(100)
AS
BEGIN
    SET NOCOUNT ON;

    INSERT INTO [dbo].[PatientDocument] (
        [Patient_ID],
        [FileName],
        [FilePath],
        [ContentType]
    )
    VALUES (
        @Patient_ID,
        @FileName,
        @FilePath,
        @ContentType
    );
END;
GO
PRINT N'Creating Procedure [dbo].[spPatientDocument_List]...';


GO
CREATE PROCEDURE [dbo].[spPatientDocument_List]
    @Patient_ID VARCHAR(100)
AS
BEGIN
    SET NOCOUNT ON;

    SELECT 
        [PatientDocument_ID],
        [Patient_ID],
        [FileName],
        [FilePath],
        [ContentType],
        [UploadedOn]
    FROM [dbo].[PatientDocument]
    WHERE [Patient_ID] = @Patient_ID
    ORDER BY [UploadedOn] DESC, [PatientDocument_ID] DESC;
END;
GO
PRINT N'Update complete.';


GO

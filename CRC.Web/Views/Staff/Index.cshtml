@{
    ViewData["Title"] = "Active Staff";
}

<div class="container mt-4">
    <div class="card mb-3">
        <div class="card-header">
            <div class="d-flex justify-content-between align-items-center mb-3">
                <h2>Staff</h2>
            </div>
            <div class="d-flex justify-content-between align-items-center mb-3">
                <button id="btnAddStaff" class="btn btn-primary" type="button">Add Staff</button>
                <div class="ms-2" style="max-width: 260px;">
                    <input id="staffSearch"
                           type="text"
                           class="form-control form-control-sm"
                           placeholder="Search by ID or Name..."
                           autocomplete="off" />
                </div>
            </div>

            <div class="table-responsive">
                <table class="table table-striped table-sm table-bordered align-middle" id="staffTable">
                    <thead class="table-secondary">
                        <tr>
                            <th scope="col" class="text-nowrap">ID</th>
                            <th scope="col">Name</th>
                            <th scope="col" class="text-nowrap">NRIC</th>
                            <th scope="col" class="text-nowrap">Phone</th>
                            <th scope="col">Email</th>
                            <th scope="col">Staff Type</th>
                            <th scope="col" class="text-center" style="width: 160px;">Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        <!-- populated by JS -->
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script src="~/js/staff/index.js"></script>
}

@{
    ViewData["Title"] = "Patient Details (Staff)";
    var pid = ViewData["PatientId"]?.ToString() ?? "";
}

<div class="card shadow-sm">
    <div class="card-body">
        <h5 class="mb-2">Patient Details (Staff View)</h5>
        <p class="text-muted mb-0">
            Placeholder page. Patient ID: <strong>@pid</strong>
        </p>
    </div>
</div>